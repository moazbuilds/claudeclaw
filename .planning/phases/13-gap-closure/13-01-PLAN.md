---
phase: 13-gap-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/executor.ts
  - src/orchestrator/resumable-jobs.ts
  - src/gateway/index.ts
  - src/event-processor.ts
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "OrchestratorGovernanceAdapter is wired to executor via setGovernanceClient() at startup"
    - "Policy denials trigger escalation via handlePolicyDenial()"
    - "DLQ overflow triggers escalation via handleDlqOverflow() when threshold exceeded"
  artifacts:
    - path: "src/orchestrator/resumable-jobs.ts"
      provides: "setGovernanceClient initialization"
      min_lines: 1
    - path: "src/gateway/index.ts"
      provides: "handlePolicyDenial integration"
      min_lines: 1
    - path: "src/event-processor.ts"
      provides: "handleDlqOverflow integration"
      min_lines: 1
---

<objective>
Wire 3 remaining integration gaps from v1.0 milestone audit:
1. OrchestratorGovernanceAdapter → call setGovernanceClient() at startup
2. Policy denial → call handlePolicyDenial() in gateway
3. DLQ overflow → call handleDlqOverflow() when threshold exceeded
</objective>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md (gap details)
@src/orchestrator/executor.ts (setGovernanceClient defined at line 88)
@src/orchestrator/governance-adapter.ts (OrchestratorGovernanceAdapter implementation)
@src/gateway/index.ts (policy denial at lines 230-234)
@src/escalation/triggers.ts (handlePolicyDenial at line 416, handleDlqOverflow at line 465)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire OrchestratorGovernanceAdapter via setGovernanceClient()</name>
  <files>src/orchestrator/resumable-jobs.ts, src/orchestrator/executor.ts</files>
  <action>
Wire setGovernanceClient() to be called when orchestrator initializes.

1. Read src/orchestrator/resumable-jobs.ts to find initialization/export patterns
2. Read src/orchestrator/executor.ts lines 83-100 to understand how governanceClient is used in checkGovernance()
3. Add initialization call in resumable-jobs.ts:
   - Import OrchestratorGovernanceAdapter from "./governance-adapter"
   - Import setGovernanceClient from "./executor"
   - Call setGovernanceClient(new OrchestratorGovernanceAdapter()) after imports or in initialization

The orchestrator needs to have its governance adapter wired at startup so task-level governance checks actually run.
  </action>
  <verify>
    <automated>grep -n "setGovernanceClient.*OrchestratorGovernanceAdapter" src/orchestrator/resumable-jobs.ts || echo "NOT FOUND"</automated>
  </verify>
  <done>
    OrchestratorGovernanceAdapter is instantiated and wired via setGovernanceClient() in production code
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire handlePolicyDenial in Gateway policy denial path</name>
  <files>src/gateway/index.ts</files>
  <action>
Wire handlePolicyDenial() to be called when policy denies a request.

1. Read src/gateway/index.ts around lines 229-235 where policy denial returns error
2. Import handlePolicyDenial from "../escalation"
3. Before returning the denial error, call:
   await handlePolicyDenial(event.id, "InboundMessage", policyDecision.reason || "Policy denied request", {
     severity: "warning",
     channelId: event.channelId,
     sessionId: sessionEntry.sessionId,
   });

This ensures policy denials trigger escalation actions (notifications, handoffs, etc.) per the escalation policy.
  </action>
  <verify>
    <automated>grep -n "handlePolicyDenial" src/gateway/index.ts || echo "NOT FOUND"</automated>
  </verify>
  <done>
    Policy denials in gateway trigger escalation via handlePolicyDenial()
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire handleDlqOverflow when DLQ threshold exceeded</name>
  <files>src/event-processor.ts</files>
  <action>
Wire handleDlqOverflow() to be called when DLQ size exceeds threshold.

1. Read src/escalation/triggers.ts lines 465-483 to understand handleDlqOverflow signature:
   handleDlqOverflow(dlqSize: number, threshold: number, options?: { eventId?, workflowId? })
2. Read src/dead-letter-queue.ts to understand:
   - DLQ size tracking (list() returns entries)
   - Threshold configuration
3. In event-processor.ts, when an event is marked as "dead_lettered" (around line 289-294):
   - Import handleDlqOverflow from "../escalation"
   - Get current DLQ size: const dlqSize = listDLQ().length
   - Use a threshold (e.g., 100) - could be configurable but hardcode reasonable default
   - If dlqSize > threshold, call handleDlqOverflow(dlqSize, threshold, { eventId: nextEvent.id })

This ensures DLQ overflow triggers escalation when the queue gets too large.
  </action>
  <verify>
    <automated>grep -n "handleDlqOverflow" src/event-processor.ts || echo "NOT FOUND"</automated>
  </verify>
  <done>
    DLQ overflow triggers escalation via handleDlqOverflow() when threshold exceeded
  </done>
</task>

</tasks>

<verification>
1. grep finds setGovernanceClient call with OrchestratorGovernanceAdapter in resumable-jobs.ts
2. grep finds handlePolicyDenial call in gateway/index.ts
3. grep finds handleDlqOverflow call in event-processor.ts
</verification>

<success_criteria>
- OrchestratorGovernanceAdapter is wired via setGovernanceClient() at startup
- Policy denials trigger handlePolicyDenial() escalation
- DLQ overflow triggers handleDlqOverflow() escalation
</success_criteria>

<output>
After completion, create `.planning/phases/13-gap-closure/13-SUMMARY.md`
</output>
